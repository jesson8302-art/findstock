# 프론트엔드는 Supabase에 직접 연결하지 않습니다.
# 모든 데이터는 백엔드 /api/* 를 통해 받아옵니다.
#
# 백엔드가 다른 서버에 있을 때만 아래를 설정하세요.
# (로컬 개발 시에는 Vite proxy가 자동으로 localhost:8000 으로 연결)
#
# VITE_API_BASE=https://your-backend-server.com
